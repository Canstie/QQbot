-- Legacy single-file Lua entry.
-- New commands should be placed in scripts/lua/<command>.lua.
-- Example: scripts/lua/抽群老婆.lua is triggered by ~抽群老婆.

function on_message(event, api)
  return nil
end
